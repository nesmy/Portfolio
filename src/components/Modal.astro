---

---

<!-- Modal -->
  <div id="projectModal" class="fixed inset-0 z-50 bg-black bg-opacity-60 flex items-center justify-center hidden">
    <div class="bg-white dark:bg-gray-800 p-6 rounded-lg max-w-xl w-full relative text-center">
      <button onclick="closeModal()" class="absolute top-2 right-2 text-gray-500 hover:text-white text-xl">&times;</button>
      <h3 id="modalTitle" class="text-2xl font-bold mb-4"></h3>
      <p id="modalDesc" class="text-gray-700 dark:text-gray-300 mb-4"></p>
      <div id="modalGallery" class="flex gap-2 justify-center mb-6"></div>
      <a id="modalLink" class="text-green-500 hover:underline" href="#" target="_blank">View Project</a>
    </div>
  </div>